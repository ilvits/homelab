# Secrets — never commit real credentials
.env
*.env
.env.save
*.env.save

# Deprecated stacks — not tracked
icloudpd/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log

# Linux open-file artifacts (FUSE)
.fuse_hidden*

# Seafile
.seafile-data
scripts/.env

# scratch files
_*.txt
*.bak

# VPS and mail-stack scripts live in homelab-private: they carry the public IP,
# mail domain, backup paths and the exact boundary of what the VPS key may do
# on Unraid.
scripts/vps/
scripts/stalwart-*.sh
scripts/ssh-forced-*.sh
docs/RESTORE-*.md
